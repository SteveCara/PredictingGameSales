
# # multiple linear regression
# lm = LinearRegression()
# lm.fit(X_train, y_train)

# # do a cross val score (like a mini train_test_split)
# lm_cv = cross_val_score(lm, X_train, y_train,
#                         scoring='neg_mean_absolute_error', cv=3)
